# 可视化功能说明

## 🎨 追踪详情可视化

Auto-Visualizer 提供了美观的可视化界面来展示方法追踪详情。

### 📍 访问方式

```bash
# 方式 1: 通过 API 获取追踪 ID
curl http://localhost:8090/api/method-traces

# 方式 2: 直接访问可视化页面
http://localhost:8090/trace/{trace_id}
```

### 🎯 页面功能

#### 1. **头部信息区**
显示追踪的核心元数据：
- 方法名称
- 所属包
- 执行状态（成功/失败/运行中）
- 总耗时
- Goroutine ID

#### 2. **执行时间线** ⏱️
以时间线形式展示方法执行过程：
- 开始时间
- 结束时间
- 总耗时
- 执行状态

#### 3. **性能指标卡片** 📊
三个醒目的指标卡片：
- 执行时间
- 执行状态
- 子调用数量

#### 4. **输入参数** 📥
格式化展示方法的输入参数：
- JSON 格式化
- 语法高亮
- 代码块展示

#### 5. **返回值** 📤
格式化展示方法的返回值：
- 返回结果
- 错误信息（如果有）
- 语法高亮

#### 6. **调用栈** 📚
完整的方法调用栈：
- 文件路径
- 行号
- 函数名
- 按调用顺序排列

#### 7. **子调用树** 🌲
树形展示嵌套的方法调用：
- 层级缩进
- 每个节点显示：
  - 方法名
  - 执行时间
  - 执行状态
- 支持多层嵌套

### 🎨 设计特点

#### 视觉效果
- ✨ 渐变色背景（紫色系）
- 🎴 卡片式布局
- 🌈 彩色状态标签
- 📏 清晰的层级结构
- 💫 流畅的动画效果

#### 颜色方案
- **成功**: 绿色 (#d4edda)
- **失败**: 红色 (#f8d7da)
- **运行中**: 黄色 (#fff3cd)
- **主色调**: 紫色渐变 (#667eea → #764ba2)

#### 响应式设计
- 📱 移动端适配
- 💻 桌面端优化
- 📐 自适应布局

### 📖 使用示例

#### 示例 1: 查看单个追踪

```bash
# 1. 执行业务方法（会自动追踪）
curl -X POST http://your-service/api/process-order

# 2. 获取追踪列表
curl http://localhost:8090/api/method-traces

# 3. 访问可视化页面
open http://localhost:8090/trace/trace_1234567890
```

#### 示例 2: 查看调用链

```go
// 业务代码
func ProcessOrder(orderID string) {
    end := autovisualizer.Begin("ProcessOrder", orderID)
    defer end("success", nil)
    
    // 子调用 1
    ValidateOrder(orderID)
    
    // 子调用 2
    SaveOrder(orderID)
}

func ValidateOrder(orderID string) {
    defer autovisualizer.Measure("ValidateOrder")()
    // 验证逻辑
}

func SaveOrder(orderID string) {
    defer autovisualizer.Measure("SaveOrder")()
    // 保存逻辑
}
```

可视化页面会展示：
```
ProcessOrder (150ms) ✅
├── ValidateOrder (50ms) ✅
└── SaveOrder (80ms) ✅
```

### 🔍 详细信息展示

#### 时间线示例
```
📅 执行时间线
├─ 开始时间: 2025-10-20 15:30:45.123
├─ 结束时间: 2025-10-20 15:30:45.273
└─ 总耗时: 150ms
```

#### 性能指标示例
```
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│   150ms     │  │   success   │  │      2      │
│  执行时间    │  │    状态     │  │  子调用数   │
└─────────────┘  └─────────────┘  └─────────────┘
```

#### 参数展示示例
```json
📥 输入参数
{
  "orderID": "ORD-001",
  "amount": 999.99,
  "userID": 12345
}

📤 返回值
{
  "success": true,
  "orderID": "ORD-001",
  "status": "processed"
}
```

### 🎯 与其他工具对比

| 功能 | Auto-Visualizer | Jaeger | Zipkin | pprof |
|------|----------------|--------|--------|-------|
| 方法追踪 | ✅ | ✅ | ✅ | ⚠️ |
| 参数展示 | ✅ | ❌ | ❌ | ❌ |
| 返回值展示 | ✅ | ❌ | ❌ | ❌ |
| 调用栈 | ✅ | ⚠️ | ⚠️ | ✅ |
| 可视化 | ✅ | ✅ | ✅ | ⚠️ |
| 零侵入 | ✅ | ❌ | ❌ | ✅ |
| 本地部署 | ✅ | ❌ | ❌ | ✅ |

### 💡 最佳实践

#### 1. 追踪关键路径
```go
// ✅ 追踪业务关键方法
func ProcessPayment(amount float64) error {
    end := autovisualizer.Begin("ProcessPayment", amount)
    defer end(nil)
    
    // 业务逻辑
    return nil
}
```

#### 2. 查看性能瓶颈
1. 访问可视化页面
2. 查看"执行时间"指标
3. 检查"子调用树"找出最慢的方法
4. 优化对应的代码

#### 3. 调试错误
1. 查看"状态"标签（红色表示失败）
2. 查看"错误信息"区域
3. 查看"调用栈"定位问题
4. 查看"输入参数"重现问题

### 🔗 相关链接

- [快速开始](QUICKSTART.md)
- [使用指南](USAGE.md)
- [API 文档](README.md#api接口)

### 📸 页面预览

访问 `http://localhost:8090/trace/{trace_id}` 可以看到：

```
┌─────────────────────────────────────────────────────┐
│  🔍 追踪详情                                         │
│  方法: ProcessOrder  包: main  状态: ✅ success      │
│  耗时: 150ms  Goroutine: #123                       │
├─────────────────────────────────────────────────────┤
│  📅 执行时间线        │  📊 性能指标                 │
│  ├─ 开始: 15:30:45   │  ┌─────────┐                │
│  ├─ 结束: 15:30:45   │  │ 150ms   │                │
│  └─ 耗时: 150ms      │  └─────────┘                │
├─────────────────────────────────────────────────────┤
│  📥 输入参数          │  📤 返回值                   │
│  {                   │  {                          │
│    "orderID": "001"  │    "success": true          │
│  }                   │  }                          │
├─────────────────────────────────────────────────────┤
│  📚 调用栈                                           │
│  #1 main.go:123 main.ProcessOrder                   │
│  #2 handler.go:45 handler.HandleRequest             │
├─────────────────────────────────────────────────────┤
│  🌲 子调用链                                         │
│  └─ ValidateOrder (50ms) ✅                         │
│  └─ SaveOrder (80ms) ✅                             │
└─────────────────────────────────────────────────────┘
```

---

**让追踪数据更直观、更易懂！** 🎨

